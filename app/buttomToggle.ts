export interface buttonInfo {
    id_name: string
    oclink: string
    TitleOrTip: string
}

export const ButtonToggle: buttonInfo[] = [
    {
        id_name: "nav-console",
        oclink: "sco.showConsole()",
        TitleOrTip: "显示中控台"
    }
]